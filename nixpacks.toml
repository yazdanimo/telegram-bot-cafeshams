[phases.setup]
nixPkgs = ['python311', 'gcc', 'pkg-config']

[phases.install]
cmds = [
    'python -m pip install --upgrade pip setuptools wheel',
    'python -m pip install -r requirements.txt'
]

[start]
cmd = 'gunicorn --bind 0.0.0.0:$PORT --timeout 120 --workers 1 --threads 2 main:flask_app'
